<%= simple_form_for(@income) do |f| %>
  <% if @income.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@income.errors.count, "error") %> prohibited this income from being saved:</h2>

      <ul>
      <% @income.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :day_of_month %><br />
    <%= f.number_field :day_of_month %>
  </div>
  <div class="field">
    <%= f.label :recurring %><br />
    <%= f.check_box :recurring %>
  </div>
  <div class="field">
    <%= f.label :every_x_weeks %><br />
    <%= f.number_field :every_x_weeks %>
  </div>
  <div class="field">
    <%= f.input :start_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
